function theta = rotationAngles(theta_x, theta_y, theta_z)
    % theta_x, theta_y, theta_z - initial rotation angles
    % theta - vector containing rotation angles for each axis

    theta(1) = theta_x;
    theta(2) = theta_y;
    theta(3) = theta_z;
end